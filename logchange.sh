#!/bin/bash

ls -l /var/log/ | grep Nov | less ##we can see what directories were changed in the month of November as an example for the most recent date. or you can change it to specify the year as well.
